ssh el627@ogg.cs.bath.ac.uk
jZPXuK5qV4Q
cd research-project
screen
# hit enter twice
# if you need to then use git pull
# b87f85dcf1653a998ce5751bd108ec206e35e449
cd research-project/
# make sure the bash script is runnable -- use 'chmod +x ogg-run.sh' etc
docker run --rm --ipc=host --runtime=nvidia -e NVIDIA_VISIBLE_DEVICES=0 -v "$(pwd)":/app -v "/mnt/slow0/ucf101/data":/data elliottloveridge/distiller /app/ogg-run.sh
# Ctrl + a followed by a 'd' to detach
# Exit the ssh session
# ssh back into the server like before
screen -r
# If your code has finished then collect the results, otherwise exit out again


ucf101 docker command:
docker run --rm --ipc=host --runtime=nvidia -e NVIDIA_VISIBLE_DEVICES=0 -v "$(pwd)":/app -v "/mnt/slow0/ucf101/data":/data elliottloveridge/distiller /app/ogg-run.sh
