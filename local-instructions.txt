NOTE: this is for running files on my local machine - different directory structure and uses no_cuda 

screen
# hit enter
cd ~/Documents/GitHub/research-project/
docker run --rm --ipc=host -v "$(pwd)":/app -v "/Users/elliottloveridge/Datasets/UCF101/data":/data el627/dist /app/local-run.sh
# Ctrl + a followed by a 'd'
# End terminal session
screen -r
# If your code has finished then collect the results, otherwise exit out again
exit


ucf101 docker commands:
train and test: docker run --rm --ipc=host -v "$(pwd)":/app -v "/Users/elliottloveridge/Datasets/UCF101/data":/data el627/dist /app/local-run.sh
evaluate: docker run --rm --ipc=host -v "$(pwd)":/app -v "/Users/elliottloveridge/Datasets/UCF101/data":/data el627/dist /app/local-eval.sh
